meta {
  name: Delete Disease Category
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/disease-categories/{{diseaseCategoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Disease Category

  Deletes a disease category by ID.
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Get Disease Category
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/disease-categories/{{diseaseCategoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get Disease Category

  Returns a single disease category by ID.
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

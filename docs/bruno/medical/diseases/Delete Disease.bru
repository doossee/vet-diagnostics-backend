meta {
  name: Delete Disease
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/diseases/{{diseaseId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Disease

  Deletes a disease by ID.
  Requires authentication.

  ## Path Parameters
  - `diseaseId` — UUID of the disease to delete
}

tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}

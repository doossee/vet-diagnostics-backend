meta {
  name: Delete Prophylaxis
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/prophylaxis/{{prophylaxisId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Prophylaxis

  Deletes a prophylaxis record by ID.
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

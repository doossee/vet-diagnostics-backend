meta {
  name: Delete Prophylaxis Detail
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/prophylaxis-details/{{prophylaxisDetailId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Prophylaxis Detail

  Deletes a prophylaxis detail by ID.
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Create Body Type
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/body-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Нормальный",
      "uz": "Normal"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save bodyTypeId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("bodyTypeId", id);
  });
}

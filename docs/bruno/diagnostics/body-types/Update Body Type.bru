meta {
  name: Update Body Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/body-types/{{bodyTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Тучный",
      "uz": "Semiz"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

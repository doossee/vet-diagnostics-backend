meta {
  name: Update Rumen Fluid State
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/rumen-fluid-states/{{rumenFluidStateId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Кислый",
      "uz": "Nordon"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

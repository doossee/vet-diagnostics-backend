meta {
  name: Create Rumen Fluid State
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/rumen-fluid-states
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Нормальный",
      "uz": "Normal"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save rumenFluidStateId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("rumenFluidStateId", id);
  });
}

meta {
  name: Get Rumen Fluid State
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/rumen-fluid-states/{{rumenFluidStateId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

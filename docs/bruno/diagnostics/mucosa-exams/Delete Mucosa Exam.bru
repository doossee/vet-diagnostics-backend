meta {
  name: Delete Mucosa Exam
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/mucosa-exams/{{mucosaExamId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

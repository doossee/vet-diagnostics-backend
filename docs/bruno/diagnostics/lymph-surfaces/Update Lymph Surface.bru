meta {
  name: Update Lymph Surface
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/lymph-surfaces/{{lymphSurfaceId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Бугристая",
      "uz": "G'adir-budur"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

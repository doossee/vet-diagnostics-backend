meta {
  name: Create Lymph Size
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/lymph-sizes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Нормальный",
      "uz": "Normal"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save lymphSizeId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("lymphSizeId", id);
  });
}

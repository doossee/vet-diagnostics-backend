meta {
  name: Update Lymph Size
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/lymph-sizes/{{lymphSizeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Увеличенный",
      "uz": "Kattalashgan"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

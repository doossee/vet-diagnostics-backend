meta {
  name: Update Urine Clarity
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/urine-clarities/{{urineClarityId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Мутная",
      "uz": "Loyqa"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

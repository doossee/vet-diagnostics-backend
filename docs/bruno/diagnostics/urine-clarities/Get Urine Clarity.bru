meta {
  name: Get Urine Clarity
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/urine-clarities/{{urineClarityId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

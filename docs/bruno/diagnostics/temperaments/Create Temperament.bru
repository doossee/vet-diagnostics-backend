meta {
  name: Create Temperament
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/temperaments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Живой",
      "uz": "Jonli"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save temperamentId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("temperamentId", id);
  });
}

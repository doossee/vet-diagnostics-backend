meta {
  name: Update Temperament
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/temperaments/{{temperamentId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Флегматичный",
      "uz": "Flegmatik"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

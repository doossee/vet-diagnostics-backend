meta {
  name: Get Feces Color
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/feces-colors/{{fecesColorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

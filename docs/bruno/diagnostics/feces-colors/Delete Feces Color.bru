meta {
  name: Delete Feces Color
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/feces-colors/{{fecesColorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

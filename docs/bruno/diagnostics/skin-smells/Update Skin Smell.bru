meta {
  name: Update Skin Smell
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/skin-smells/{{skinSmellId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Кислый",
      "uz": "Nordon"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

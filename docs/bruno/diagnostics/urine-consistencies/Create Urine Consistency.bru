meta {
  name: Create Urine Consistency
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/urine-consistencies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Водянистая",
      "uz": "Suvli"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save urineConsistencyId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("urineConsistencyId", id);
  });
}

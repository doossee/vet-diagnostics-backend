meta {
  name: Update Urine Consistency
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/urine-consistencies/{{urineConsistencyId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Густая",
      "uz": "Qalin"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

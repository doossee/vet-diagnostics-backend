meta {
  name: Create Lymph Pain
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/lymph-pains
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Безболезненный",
      "uz": "Og'riqsiz"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save lymphPainId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("lymphPainId", id);
  });
}

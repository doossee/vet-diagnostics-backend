meta {
  name: Create Mucosa Type
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/mucosa-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Конъюнктива",
      "uz": "Konjunktiva"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save mucosaTypeId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("mucosaTypeId", id);
  });
}

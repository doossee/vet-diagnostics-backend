meta {
  name: Update Mucosa Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/mucosa-types/{{mucosaTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Ротовая",
      "uz": "Og'iz"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

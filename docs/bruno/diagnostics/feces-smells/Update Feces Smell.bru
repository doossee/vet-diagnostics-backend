meta {
  name: Update Feces Smell
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/feces-smells/{{fecesSmellId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Зловонный",
      "uz": "Badbo'y"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

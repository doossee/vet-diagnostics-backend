meta {
  name: Update Lymph Temp
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/lymph-temps/{{lymphTempId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Повышенная",
      "uz": "Oshgan"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

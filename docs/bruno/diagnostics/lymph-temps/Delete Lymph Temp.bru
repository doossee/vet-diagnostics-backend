meta {
  name: Delete Lymph Temp
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/lymph-temps/{{lymphTempId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

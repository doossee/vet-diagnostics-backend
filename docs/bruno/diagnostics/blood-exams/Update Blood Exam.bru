meta {
  name: Update Blood Exam
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/blood-exams/{{bloodExamId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "hemoglobin": 125,
    "glucose": 3.8
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

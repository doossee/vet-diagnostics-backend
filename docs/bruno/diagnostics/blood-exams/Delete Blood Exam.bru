meta {
  name: Delete Blood Exam
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/blood-exams/{{bloodExamId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

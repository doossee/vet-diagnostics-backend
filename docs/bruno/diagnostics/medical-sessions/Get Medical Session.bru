meta {
  name: Get Medical Session
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/medical-sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

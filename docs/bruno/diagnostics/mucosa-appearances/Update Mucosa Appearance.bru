meta {
  name: Update Mucosa Appearance
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/mucosa-appearances/{{mucosaAppearanceId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Гиперемированная",
      "uz": "Giperemiyalangan"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Get Skin Color
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/skin-colors/{{skinColorId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

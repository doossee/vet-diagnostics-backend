meta {
  name: Delete Skin Humidity
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/skin-humidities/{{skinHumidityId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

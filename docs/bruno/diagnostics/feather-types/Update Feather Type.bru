meta {
  name: Update Feather Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/feather-types/{{featherTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Рыхлое",
      "uz": "Bo'sh"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

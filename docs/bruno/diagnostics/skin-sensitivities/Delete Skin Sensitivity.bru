meta {
  name: Delete Skin Sensitivity
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/skin-sensitivities/{{skinSensitivityId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

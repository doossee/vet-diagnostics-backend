meta {
  name: Update Skin Sensitivity
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/skin-sensitivities/{{skinSensitivityId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Повышенная",
      "uz": "Oshgan"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Get Skin Sensitivity
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/skin-sensitivities/{{skinSensitivityId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

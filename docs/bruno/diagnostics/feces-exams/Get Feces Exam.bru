meta {
  name: Get Feces Exam
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/feces-exams/{{fecesExamId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

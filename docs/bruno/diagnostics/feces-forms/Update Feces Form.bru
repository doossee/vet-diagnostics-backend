meta {
  name: Update Feces Form
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/feces-forms/{{fecesFormId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Неоформленный",
      "uz": "Shakllanmagan"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

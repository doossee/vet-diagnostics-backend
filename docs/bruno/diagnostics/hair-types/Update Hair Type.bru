meta {
  name: Update Hair Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/hair-types/{{hairTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Жёсткий",
      "uz": "Qattiq"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

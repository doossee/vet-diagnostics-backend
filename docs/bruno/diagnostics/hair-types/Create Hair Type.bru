meta {
  name: Create Hair Type
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/hair-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Гладкий",
      "uz": "Silliq"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save hairTypeId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("hairTypeId", id);
  });
}

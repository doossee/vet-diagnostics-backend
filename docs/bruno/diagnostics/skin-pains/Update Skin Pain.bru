meta {
  name: Update Skin Pain
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/skin-pains/{{skinPainId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Болезненная",
      "uz": "Og'riqli"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

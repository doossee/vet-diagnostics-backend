meta {
  name: Delete Skin Pain
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/skin-pains/{{skinPainId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Get Reference Range
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/reference-ranges/{{referenceRangeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Update Lymph Mobility
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/lymph-mobilities/{{lymphMobilityId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Неподвижный",
      "uz": "Harakatsiz"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Update Body Position
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/body-positions/{{bodyPositionId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Лежачее",
      "uz": "Yotish"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

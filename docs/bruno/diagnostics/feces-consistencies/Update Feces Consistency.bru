meta {
  name: Update Feces Consistency
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/feces-consistencies/{{fecesConsistencyId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Жидкая",
      "uz": "Suyuq"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Delete Skin Surface
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/skin-surfaces/{{skinSurfaceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: Get Lymph Consistency
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/lymph-consistencies/{{lymphConsistencyId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

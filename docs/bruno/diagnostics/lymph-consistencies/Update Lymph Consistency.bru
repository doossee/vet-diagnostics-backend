meta {
  name: Update Lymph Consistency
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/lymph-consistencies/{{lymphConsistencyId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Плотная",
      "uz": "Zich"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

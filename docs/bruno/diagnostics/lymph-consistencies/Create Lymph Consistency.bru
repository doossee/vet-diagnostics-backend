meta {
  name: Create Lymph Consistency
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/lymph-consistencies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Упругая",
      "uz": "Elastik"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save lymphConsistencyId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("lymphConsistencyId", id);
  });
}

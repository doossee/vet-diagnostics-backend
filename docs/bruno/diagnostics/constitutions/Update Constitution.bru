meta {
  name: Update Constitution
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/constitutions/{{constitutionId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Слабая",
      "uz": "Zaif"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

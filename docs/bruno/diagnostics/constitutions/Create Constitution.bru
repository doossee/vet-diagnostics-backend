meta {
  name: Create Constitution
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/constitutions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Крепкая",
      "uz": "Mustahkam"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save constitutionId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("constitutionId", id);
  });
}

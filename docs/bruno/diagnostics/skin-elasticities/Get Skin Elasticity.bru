meta {
  name: Get Skin Elasticity
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/skin-elasticities/{{skinElasticityId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

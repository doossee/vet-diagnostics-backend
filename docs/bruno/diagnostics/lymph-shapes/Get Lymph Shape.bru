meta {
  name: Get Lymph Shape
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/lymph-shapes/{{lymphShapeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

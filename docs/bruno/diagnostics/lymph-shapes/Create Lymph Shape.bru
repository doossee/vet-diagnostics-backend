meta {
  name: Create Lymph Shape
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/lymph-shapes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Овальный",
      "uz": "Oval"
    },
    "numericValue": 1
  }
}

tests {
  test("Status 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Save lymphShapeId", function() {
    const id = res.body.data?.id;
    expect(id).to.be.a("string");
    bru.setVar("lymphShapeId", id);
  });
}

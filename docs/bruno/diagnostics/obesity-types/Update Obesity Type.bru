meta {
  name: Update Obesity Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/obesity-types/{{obesityTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Ожирение",
      "uz": "Semizlik"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

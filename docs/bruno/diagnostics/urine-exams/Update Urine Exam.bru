meta {
  name: Update Urine Exam
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/urine-exams/{{urineExamId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "ph": 7.8,
    "amount": 9.0
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

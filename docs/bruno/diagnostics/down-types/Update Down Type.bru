meta {
  name: Update Down Type
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/down-types/{{downTypeId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": {
      "ru": "Редкий",
      "uz": "Siyrak"
    },
    "numericValue": 2
  }
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

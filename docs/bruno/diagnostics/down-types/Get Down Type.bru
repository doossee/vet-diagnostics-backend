meta {
  name: Get Down Type
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/down-types/{{downTypeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

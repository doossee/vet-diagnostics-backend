meta {
  name: Delete Vet Station
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/vet-stations/{{vetStationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Delete Vet Station

  Deletes a veterinary station by ID. Requires admin access.

  ## Path Parameters
  - `id` — Vet station UUID

  ## Response
  Returns the deleted `VetStationEntity`.
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}

meta {
  name: List Animal Sexes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/animal-sexes?page=1&perPage=10
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Data is array", function() {
    expect(res.body.data).to.be.an("array");
  });
}

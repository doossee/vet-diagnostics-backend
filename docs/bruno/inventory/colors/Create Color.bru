meta {
  name: Create Color
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/colors
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": {
      "ru": "Белый",
      "uz": "Oq"
    }
  }
}

docs {
  # Create Color

  Creates a new animal coat color entry with a bilingual name.

  **Required fields:**
  - `name` — bilingual name object with `ru` and `uz` keys
}

tests {
  test("Status is 201", function() {
    expect(res.status).to.equal(201);
  });
  test("Returns id", function() {
    expect(res.body.data).to.have.property("id");
    bru.setVar("colorId", res.body.data.id);
  });
}

meta {
  name: Get Animal Type
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/animal-types/{{animalTypeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

docs {
  # Get Animal Type

  Returns a single animal type by its ID.

  ## Path Parameters
  - `animalTypeId` — UUID of the animal type
}

tests {
  test("Status 200", function() {
    expect(res.status).to.equal(200);
  });
}
